# Python
__pycache__/
*.py[cod]
*.egg-info/

# Isaac Lab / Hydra / training output
logs/
outputs/
wandb/
runs/

# Local environment/cache
.pytest_cache/
.ruff_cache/
.mypy_cache/
_isaac_sim/

# Stray root-owned duplicate export dir left by a container run; contents
# already live under ExportedPolicy/longer_training/. Undeletable without
# sudo on the host, so just keep it out of git status.
ExportedPolicy/exported/
